{
  "name": "server",
  "version": "1.0.0",
  "description": "A mock API Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-server": "json-server --watch ./database/data.json"
  },
  "author": "taseenjeem",
  "license": "MIT",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
